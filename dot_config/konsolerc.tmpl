{{/* Konsole base config — only applied on Bazzite (KDE host).
     Arch host runs Hyprland, so this file is gated via .chezmoiignore. */}}
[Desktop Entry]
DefaultProfile=Distrobox.profile

[General]
ConfigVersion=1

[UiSettings]
ColorScheme=
