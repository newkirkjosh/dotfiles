# Night profile. Deliberately minimal: colors only, everything else
# inherits Konsole's built-in defaults so switching profiles never
# changes font, scrollback or behaviour — only the palette.

[General]
Name=RosePine
Parent=FALLBACK/

[Appearance]
ColorScheme=rose-pine
