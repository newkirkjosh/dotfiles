# Day profile. Kept byte-for-byte equivalent to RosePine.profile apart
# from the colour scheme, so a mid-session switch is purely visual.

[General]
Name=RosePineMoon
Parent=FALLBACK/

[Appearance]
ColorScheme=rose-pine-moon
